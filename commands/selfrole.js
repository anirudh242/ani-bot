// TODO: Fix later

/*const autoRole = require("discord-auto-role");
const bot = require("discord.js");

module.exports = {
  name: "selfrole",
  description: "",

  execute(message, args) {
      bot.Con
    autoRole(bot, {
      prefix: "-",
      roles: {
        australia: "Australia",
        austria: "Austria",
      },
      helpcmd: "location",
      msg:
        "Please use one of the following command to assign or remove the desired group:",
      prunetimer: "30000",
    });
  },
};
*/
